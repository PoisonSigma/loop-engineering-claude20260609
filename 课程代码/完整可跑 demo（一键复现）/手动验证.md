档 1️⃣ 零成本:看现成的真跑产物(不装任何东西、不花 token)

直接验证「真 AI 没作弊」——进我留档的那个带 git 历史的仓库:

cd "/Users/poison/claude design B站去ai化情报爬取实战课/loop-engineering-claude/课程代码/第 3 课： Step 5 · 一个自动化（headless，带硬门+硬停止）/真 claude-p 实测产物-loop-demo"

```
git log --oneline          # 看到 failing baseline = AI 动手前
git diff --stat            # 只动了 src/calc.py + STATE.md
git diff test_calc.py      # 空 → ✅ 没碰测试
git diff src/calc.py       # 看 AI 怎么改的 divide
```









```
pip install pytest ruff
```

```
cd "/Users/poison/claude design B站去ai化情报爬取实战课/loop-engineering-claude/课程代码/完整可跑 demo（一键复现）"
```

```
bash setup_and_run.sh
```

