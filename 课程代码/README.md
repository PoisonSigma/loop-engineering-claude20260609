# 循环工程落地课 · 可运行代码 + 真实运行结果

本目录把《loop-engineering-claude 文字版笔记.md》里**能真正跑通**的代码全部抽出来，
按章节号 / Step 建文件夹，每个文件夹里放：

- `code.sh` —— 该 Step 的命令（可直接复制运行 / `bash code.sh`）
- `运行结果.md` —— **在本机实测**的真实输出（不是杜撰）

实测环境：macOS（Darwin 23.6）· Python 3.13.7 · pytest 9.1.1 · ruff 0.15.20 · git 2.39。

---

## 哪些章节有可运行代码？

| 章节 | 内容 | 有可运行代码？ |
|---|---|---|
| 第 0 课 · 先判定该不该建 | 判定清单、表格 | ❌ 纯概念，无代码 |
| 第 1 课 · 五构件映射 | 能力对照表 | ❌ 纯概念，无代码 |
| 第 2 课 · 最小可用循环 = 四件套 | 概念图 | ❌ 纯概念，无代码 |
| **第 3 课 · lint-and-fix 循环** | 手把手 6 个 Step | ✅ **全部可跑通**（本目录主体） |
| 第 4 课 · 避坑 + 安全税 | 三大失败模式、五条安全税 | ❌ 纯概念，无代码 |

所以**可运行代码集中在第 3 课**，共 6 个 Step，已逐条实测。





## 一键复现

```bash
cd "完整可跑 demo（一键复现）"
bash setup_and_run.sh
```

它会从零搭出 `loop-demo`，跑完整四件套循环（用一个 stub 模拟 `claude -p` 的修复动作），
最后门变绿、`exit 0`。详见该目录下的 `运行结果.md`。

