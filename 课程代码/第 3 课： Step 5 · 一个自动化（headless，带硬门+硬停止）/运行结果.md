# 运行结果 · Step 5 · 一个自动化（headless，带硬门+硬停止）

这步有两个文件：
- `loop.sh` —— **真正上线用的版本**，里面调 `claude -p`（需本机 claude CLI + token，会真实消耗）。
  **本步骤已用真 `claude -p` 实测通过，见下方第 3 节。**
- `loop_demo_stub.sh` —— **可离线跑通的演示版**，用 stub 假冒 Agent 的「修 src/」动作，
  其余 `for 1 2 3` / 硬门 / 退出码全是 `loop.sh` 的真实逻辑（不烧 token，用于先验证骨架）。

## 1) 先做语法自检（真实）

```bash
bash -n loop.sh
```

输出：无报错 → `[loop.sh syntax OK]`。脚本结构合法。

## 2) 跑通循环控制流（真实，用 stub，不烧 token）

```bash
cd loop-demo
bash loop_demo_stub.sh
```

真实输出：

```text
=== round 1 ===
[stub claude] 模拟 lint-fix Agent：只改 src/ ...
..                                                                       [100%]
2 passed in 0.00s
All checks passed!
1 file already formatted
✅ 门通过，停止
[loop final exit: 0]
```

解读：
- `=== round 1 ===` → `for i in 1 2 3` 的第 1 轮（硬停止上限 = 3 轮）
- Agent 修完 → 三道门齐跑：`pytest`(2 passed) + `ruff check`(All checks passed) + `ruff format --check`(already formatted)
- 三门全绿 → `✅ 门通过，停止` → **`exit 0`**（由门的退出码决定，不是 Agent 嘴上说"做完了"）

## 3) 真实 `claude -p` 实测（✅ 已真跑，非推测）

环境：claude 2.1.193 · 全新失败仓库 · 真 `claude -p`（**不是 stub**，真实消耗 token）。
直接跑真版 `loop.sh`（gate = `pytest && ruff check src/ && ruff format --check src/`）：

```bash
cd loop-demo
./loop.sh        # 真实调用 claude -p
```

真实输出（节选）：

```text
=== round 1 ===
完成。
- src/calc.py 两处修改：add 加了空格符合格式规范；divide 在 b == 0 时返回 None。
- pytest 2/2 通过，ruff check 零报错，ruff format --check 干净。
..                                                                       [100%]
2 passed in 0.00s
All checks passed!
1 file already formatted
✅ 门通过，停止
[loop.sh final exit=0]
```

**第 1 轮就过门。** 关键是：真 Agent 是否守住了契约？逐条验证：

| 验证点 | 结果 |
|---|---|
| AI 自己读 SKILL.md + STATE.md、自己修对 bug | ✅ `divide` 改成 `b==0 → return None`（正按测试契约，不是瞎改） |
| **没作弊**：只改 `src/`、没碰 `test_*.py` | ✅ `git diff` 只动 `src/calc.py` + `STATE.md`；`git diff test_calc.py` 为空 |
| 自己更新 STATE.md | ✅ 写了 Last run（PASS）、Lessons learned（divide 契约：b==0 返回 None） |

AI 改后的 `src/calc.py`：

```python
def add(a, b):
    return a + b


def divide(a, b):
    if b == 0:
        return None
    return a / b
```

`git diff --stat`（相对失败基线，`test_calc.py` 未出现）：

```text
 STATE.md    | 7 +++++--
 src/calc.py | 8 ++++++--
 2 files changed, 11 insertions(+), 4 deletions(-)
```

> 结论：**stub 版与真 AI 版结果一致** —— 这套四件套循环的设计成立，
> 真 Agent 能在最小授权（`--allowedTools` 只给 pytest/ruff）下完成任务、且守住「禁碰测试」红线。
> 注意这次任务很简单，**1 轮、几步就过**，没用到 `--max-turns 15` / `for 1 2 3` 上限——
> 硬停止闸这次没被触发，但它是给「AI 修不出来时」兜底的（没出事 ≠ 没用）。

## 4) 三道闸验证（对应第 4 课「安静失败」）

| 闸 | 在代码里 | 实测效果 |
|---|---|---|
| 真门（客观信号） | `pytest && ruff check && ruff format --check` 的退出码 | ✅ 绿=0 / ❌ 非0 |
| 防软完成 | `exit 0` 只在门绿时触发 | ✅ Agent 说完成无用，门不绿就下一轮 |
| 硬停止 | `for i in 1 2 3` + `--max-turns 15` | ✅ 循环≤3 轮、单轮≤15 步 |

## 结论

✅ Step 5 跑通：自动化骨架（心跳轮次 + 硬门 + 硬停 + 最小授权 `--allowedTools`）真实可运行，
且**已用真 `claude -p` 端到端实测**（第 3 节）——真 Agent 第 1 轮过门、守住「禁碰测试」红线。
stub 版与真 AI 版结果一致，证明这套四件套循环的设计成立。
