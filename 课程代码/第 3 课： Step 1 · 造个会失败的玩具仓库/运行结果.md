# 运行结果 · Step 1 · 造个会失败的玩具仓库

实测环境：Python 3.13.7 · pytest 9.1.1 · ruff 0.15.20 · git 2.39。

## 命令

```bash
bash code.sh
```

`bash code.sh` 执行时，自动帮你安装 `pytest` 和 `ruff` 到当前 Python 环境，因为code.sh 里面有pip install pytest ruff -q

运行完以后会生成

## 真实输出

```text
Initialized empty Git repository in .../loop-demo/.git/
```

`pip install pytest ruff` 成功（本机因 pip.conf 里失效的 cert，改用了
`pip install --cert /etc/ssl/cert.pem pytest ruff`）：

```text
pytest 9.1.1
ruff 0.15.20
```

## 生成的目录结构

```text
loop-demo/
├── .claude/
│   └── skills/
│       └── lint-fix/        # 占位，Step 3 往里写 SKILL.md
├── src/
│   └── calc.py              # 故意：divide 不处理除零 + 难看的格式
└── test_calc.py             # test_divide_zero 期望 None，当前会抛异常
```

## 结论

✅ Step 1 跑通。仓库已就位，且**故意埋了 1 个会失败的测试**（除零），
给后面的「门」一个真能判失败的靶子。
