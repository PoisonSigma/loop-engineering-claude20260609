# 运行结果 · Step 3 · 一个 skill

## 命令

```bash
cd loop-demo
bash ../code.sh
ls .claude/skills/lint-fix/
```

## 真实输出

```text
SKILL.md
```

写出的 `.claude/skills/lint-fix/SKILL.md` 内容即 `code.sh` 里 heredoc 的全文，
含 YAML frontmatter（`name` / `description`）+ Goal / Fix patterns / Never do / State 四节。

> 说明：相比原笔记，Goal 一节补了 `ruff format --check src/` 干净这一条，
> 与 Step 2 勘误一致（格式门要靠 `ruff format`）。

## 结论

✅ Step 3 跑通。skill 文件就是循环的「**长期记忆 + 行为契约**」：
- 契约：只改 `src/`、禁碰 `test_*.py`（防作弊）
- 每轮要更新 `STATE.md`

这步只写文件、不执行逻辑，所以没有程序输出，验证点是文件已生成且内容正确。
